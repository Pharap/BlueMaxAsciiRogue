void saveStatus(){
/*
  EEPROM.update(33,hx);
  EEPROM.update(34,hy);
  EEPROM.update(35,im);
  EEPROM.update(36,dlv);
  EEPROM.update(37,st);
  EEPROM.update(38,stm);
  EEPROM.update(39,lv);
  EEPROM.update(40,hconf);
  EEPROM.update(41,hblnd);
  EEPROM.update(42,hhall);
  EEPROM.update(43,hfast);
  EEPROM.update(44,hslep);
  EEPROM.update(45,hmdet);
  EEPROM.update(46,hisee);
  EEPROM.update(47,hlevi);
  EEPROM.update(48,hheld);
  EEPROM.update(49,rdex);
  EEPROM.update(50,rstr);
  EEPROM.update(51,au);
  EEPROM.update(55,hp);
  EEPROM.update(59,hpm);
  EEPROM.update(63,ex);
  EEPROM.update(67,ht);
  EEPROM.update(71,hh);
  EEPROM.update(75,nl);
  EEPROM.update(55,hp);
  for(int i=0; i<20; i++){    //from 59 to 158
    EEPROM.update(59+i*5,ii[i]);
    EEPROM.update(60+i*5,ii[i]);
    EEPROM.update(61+i*5,ii[i]);
    EEPROM.update(62+i*5,ii[i]);
    EEPROM.update(63+i*5,ii[i]);
  }
  for(int i=0; i<POMAX; i++){ //from 158 to 171
    EEPROM.update(158+i,ptab[i]);
  }
  for(int i=0; i<SCMAX; i++){ //from 172 to 183
    EEPROM.update(172+i,stab[i]);
  }
  for(int i=0; i<RGMAX; i++){ //from 184 to 194
    EEPROM.update(184+i,rtab[i]);
  }
  for(int i=0; i<POMAX; i++){ //from 195 to 204
    EEPROM.update(195+i,ptab[i]);
  }
  EEPROM.update(205,pknow);
  EEPROM.update(207,sknow);
  EEPROM.update(209,rknow);
  EEPROM.update(211,wknow);
*/
}

